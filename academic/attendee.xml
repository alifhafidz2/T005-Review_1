<odoo>
    <data>

        <!-- Attendee Tree View -->
        <record id="view_academic_attendee_tree" model="ir.ui.view">
            <field name="name">academic.attendee.tree</field>
            <field name="model">academic.attendee</field>
            <field name="type">tree</field>
            <field name="arch" type="xml">
                <tree string="Attendee">
                    <field name="name"/>
                    <field name="session_id"/>
                    <field name="partner_id"/>
                </tree>
            </field>
        </record>

        <!-- Search View -->
        <record id="attendee_search" model="ir.ui.view">
            <field name="name">attendee_search</field>
            <field name="model">academic.attendee</field>
            <field name="arch" type="xml">
                <search string="Search Attendee">
                    <field name="session_id"/>
                    <field name="name"/>
                    <group string="Group By...">
                        <filter name="course_search" string="Course" icon="terp-personal" domain="[]" context="{'group_by':'course_id'}"/>
                        <filter name="session_search" string="Session" icon="terp-personal" domain="[]" context="{'group_by':'session_id'}"/>
                        <filter name="partner_search" string="Partner" icon="terp-personal" domain="[]" context="{'group_by':'partner_id'}"/>
                    </group>
                </search>
            </field>
        </record>

    </data>
</odoo>